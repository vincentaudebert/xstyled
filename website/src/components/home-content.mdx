import { LiveEditor } from './LiveEditor'

## Use theme with natural CSS syntax

With xstyled, CSS is naturally binded to your theme. No more `${(p) => p.theme.xxx`
in your code. Your CSS remains clear and consistent.

<LiveEditor />
